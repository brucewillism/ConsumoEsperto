{
  "name": "consumoesperto-e2e",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:lint": "playwright test --list",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "typescript": "~5.7.2"
  }
}
