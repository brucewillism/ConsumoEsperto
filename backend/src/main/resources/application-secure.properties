# =============================================================================
# CONFIGURAÇÕES SEGURAS - CONSUMO ESPERTO
# =============================================================================
# Este arquivo contém configurações sensíveis que devem ser definidas
# como variáveis de ambiente em produção
# =============================================================================

# =============================================================================
# CONFIGURAÇÕES DE BANCO DE DADOS
# =============================================================================
# IMPORTANTE: Configure estas variáveis de ambiente em produção
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/consumoesperto}
spring.datasource.username=${DATABASE_USERNAME:bruce}
spring.datasource.password=${DATABASE_PASSWORD:0301HeX@}

# =============================================================================
# CONFIGURAÇÕES JWT E SEGURANÇA
# =============================================================================
# IMPORTANTE: Use uma chave secreta forte em produção
jwt.secret=${JWT_SECRET:minha_chave_secreta_jwt_super_segura_para_consumo_esperto_2024_producao}
jwt.expiration=${JWT_EXPIRATION:86400000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}
jwt.issuer=${JWT_ISSUER:consumo-esperto-api}
jwt.audience=${JWT_AUDIENCE:consumo-esperto-client}

# =============================================================================
# CONFIGURAÇÕES GOOGLE OAUTH2
# =============================================================================
# IMPORTANTE: Configure suas credenciais reais do Google
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:593452038228-47k24odoa6f18c78e3ssp9bhu56gugnm.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}

# =============================================================================
# CONFIGURAÇÕES MERCADO PAGO
# =============================================================================
# IMPORTANTE: Configure suas credenciais reais do Mercado Pago
bank.api.mercadopago.client-id=${MERCADOPAGO_CLIENT_ID:4223603750190943}
bank.api.mercadopago.client-secret=${MERCADOPAGO_CLIENT_SECRET:YOUR_MERCADOPAGO_CLIENT_SECRET}
bank.api.mercadopago.user-id=${MERCADOPAGO_USER_ID:}
bank.api.mercadopago.sandbox=${MERCADOPAGO_SANDBOX:true}

# =============================================================================
# CONFIGURAÇÕES NUBANK
# =============================================================================
# IMPORTANTE: Configure suas credenciais reais do Nubank
bank.api.nubank.client-id=${NUBANK_CLIENT_ID:}
bank.api.nubank.client-secret=${NUBANK_CLIENT_SECRET:}

# =============================================================================
# CONFIGURAÇÕES ITAÚ
# =============================================================================
# IMPORTANTE: Configure suas credenciais reais do Itaú
bank.api.itau.client-id=${ITAU_CLIENT_ID:}
bank.api.itau.client-secret=${ITAU_CLIENT_SECRET:}

# =============================================================================
# CONFIGURAÇÕES INTER
# =============================================================================
# IMPORTANTE: Configure suas credenciais reais do Inter
bank.api.inter.client-id=${INTER_CLIENT_ID:}
bank.api.inter.client-secret=${INTER_CLIENT_SECRET:}

# =============================================================================
# CONFIGURAÇÕES NGROK
# =============================================================================
# IMPORTANTE: Configure sua URL pública real em produção
ngrok.url=${NGROK_URL:https://f03db4701c9f.ngrok-free.app}
ngrok.auto-detect=${NGROK_AUTO_DETECT:true}

# =============================================================================
# CONFIGURAÇÕES DE PRODUÇÃO
# =============================================================================
# Configurações específicas para ambiente de produção
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
logging.level.root=${LOG_LEVEL_ROOT:WARN}
logging.level.com.consumoesperto=${LOG_LEVEL_APP:INFO}

# =============================================================================
# CONFIGURAÇÕES DE PERFORMANCE
# =============================================================================
# Configurações de performance baseadas no ambiente
spring.datasource.hikari.maximum-pool-size=${HIKARI_MAX_POOL_SIZE:20}
spring.datasource.hikari.minimum-idle=${HIKARI_MIN_IDLE:5}
spring.cache.caffeine.spec=${CACHE_SPEC:maximumSize=2000,expireAfterWrite=2h,recordStats}

# =============================================================================
# CONFIGURAÇÕES DE MONITORAMENTO
# =============================================================================
# Configurações de monitoramento e observabilidade
management.endpoints.web.exposure.include=${MANAGEMENT_ENDPOINTS:health,info,metrics}
management.endpoint.health.show-details=${HEALTH_DETAILS:when-authorized}

# =============================================================================
# CONFIGURAÇÕES DE EMAIL (OPCIONAL)
# =============================================================================
# Para envio de emails em produção
spring.mail.host=${EMAIL_HOST:}
spring.mail.port=${EMAIL_PORT:587}
spring.mail.username=${EMAIL_USERNAME:}
spring.mail.password=${EMAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=${EMAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${EMAIL_STARTTLS:true}

# =============================================================================
# CONFIGURAÇÕES DE BACKUP (OPCIONAL)
# =============================================================================
# Para backup automático em produção
backup.enabled=${BACKUP_ENABLED:false}
backup.schedule=${BACKUP_SCHEDULE:0 0 2 * * ?}
backup.retention-days=${BACKUP_RETENTION:30}

# =============================================================================
# CONFIGURAÇÕES DE NOTIFICAÇÕES (OPCIONAL)
# =============================================================================
# Para notificações push em produção
notifications.enabled=${NOTIFICATIONS_ENABLED:false}
notifications.firebase.server-key=${FIREBASE_SERVER_KEY:}
notifications.firebase.sender-id=${FIREBASE_SENDER_ID:}

# =============================================================================
# CONFIGURAÇÕES DE ARQUIVOS (OPCIONAL)
# =============================================================================
# Para upload de arquivos em produção
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:10MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:10MB}
file.upload.path=${FILE_UPLOAD_PATH:./uploads}

# =============================================================================
# CONFIGURAÇÕES DE REDIS (OPCIONAL)
# =============================================================================
# Para cache distribuído com Redis em produção
spring.redis.host=${REDIS_HOST:}
spring.redis.port=${REDIS_PORT:6379}
spring.redis.password=${REDIS_PASSWORD:}
spring.redis.timeout=${REDIS_TIMEOUT:2000ms}

# =============================================================================
# CONFIGURAÇÕES DE RABBITMQ (OPCIONAL)
# =============================================================================
# Para processamento assíncrono com RabbitMQ em produção
spring.rabbitmq.host=${RABBITMQ_HOST:}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

# =============================================================================
# CONFIGURAÇÕES DE SEGURANÇA AVANÇADA
# =============================================================================
# Configurações de segurança para produção
security.require-ssl=${REQUIRE_SSL:false}
security.enable-csrf=${ENABLE_CSRF:true}
security.headers.content-security-policy=${CSP_POLICY:default-src 'self'}
security.headers.strict-transport-security=${HSTS_POLICY:max-age=31536000}

# =============================================================================
# CONFIGURAÇÕES DE AUDITORIA
# =============================================================================
# Configurações de auditoria para produção
audit.enabled=${AUDIT_ENABLED:true}
audit.log-failed-logins=${AUDIT_FAILED_LOGINS:true}
audit.log-successful-logins=${AUDIT_SUCCESSFUL_LOGINS:true}
audit.log-sensitive-operations=${AUDIT_SENSITIVE_OPERATIONS:true}

# =============================================================================
# CONFIGURAÇÕES DE RATE LIMITING
# =============================================================================
# Configurações de rate limiting para produção
rate.limit.enabled=${RATE_LIMIT_ENABLED:true}
rate.limit.requests-per-minute=${RATE_LIMIT_RPM:100}
rate.limit.burst-capacity=${RATE_LIMIT_BURST:200}

# =============================================================================
# CONFIGURAÇÕES DE CIRCUIT BREAKER
# =============================================================================
# Configurações de circuit breaker para produção
resilience4j.circuitbreaker.instances.default.failure-rate-threshold=${CIRCUIT_BREAKER_THRESHOLD:50}
resilience4j.circuitbreaker.instances.default.wait-duration-in-open-state=${CIRCUIT_BREAKER_WAIT:30s}
resilience4j.retry.instances.default.max-attempts=${RETRY_MAX_ATTEMPTS:3}

# =============================================================================
# CONFIGURAÇÕES DE TRACING
# =============================================================================
# Configurações de tracing distribuído para produção
management.tracing.sampling.probability=${TRACING_SAMPLING:0.1}
management.tracing.zipkin.endpoint=${ZIPKIN_ENDPOINT:}
management.tracing.jaeger.endpoint=${JAEGER_ENDPOINT:}

# =============================================================================
# CONFIGURAÇÕES DE MÉTRICAS
# =============================================================================
# Configurações de métricas para produção
management.metrics.export.prometheus.enabled=${PROMETHEUS_ENABLED:true}
management.metrics.export.influx.enabled=${INFLUX_ENABLED:false}
management.metrics.export.graphite.enabled=${GRAPHITE_ENABLED:false}

# =============================================================================
# CONFIGURAÇÕES DE LOGGING
# =============================================================================
# Configurações de logging para produção
logging.level.org.hibernate.SQL=${HIBERNATE_SQL_LOG:WARN}
logging.level.org.springframework.security=${SECURITY_LOG_LEVEL:WARN}
logging.level.org.springframework.web=${WEB_LOG_LEVEL:WARN}
logging.level.com.consumoesperto=${APP_LOG_LEVEL:INFO}

# =============================================================================
# CONFIGURAÇÕES DE CORS
# =============================================================================
# Configurações de CORS para produção
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:4200,https://f03db4701c9f.ngrok-free.app}
cors.allowed-methods=${CORS_ALLOWED_METHODS:GET,POST,PUT,DELETE,OPTIONS}
cors.allowed-headers=${CORS_ALLOWED_HEADERS:Authorization,Content-Type,X-Requested-With}
cors.allow-credentials=${CORS_ALLOW_CREDENTIALS:true}

# =============================================================================
# CONFIGURAÇÕES DE SESSION
# =============================================================================
# Configurações de sessão para produção
server.servlet.session.timeout=${SESSION_TIMEOUT:30m}
server.servlet.session.cookie.http-only=${SESSION_HTTP_ONLY:true}
server.servlet.session.cookie.secure=${SESSION_SECURE:false}
server.servlet.session.cookie.same-site=${SESSION_SAME_SITE:strict}

# =============================================================================
# CONFIGURAÇÕES DE COMPRESSÃO
# =============================================================================
# Configurações de compressão para produção
server.compression.enabled=${COMPRESSION_ENABLED:true}
server.compression.mime-types=${COMPRESSION_MIME_TYPES:text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json}
server.compression.min-response-size=${COMPRESSION_MIN_SIZE:1024}

# =============================================================================
# CONFIGURAÇÕES DE TIMEOUT
# =============================================================================
# Configurações de timeout para produção
server.connection-timeout=${CONNECTION_TIMEOUT:20000}
server.tomcat.connection-timeout=${TOMCAT_CONNECTION_TIMEOUT:20000}
server.tomcat.keep-alive-timeout=${TOMCAT_KEEP_ALIVE_TIMEOUT:20000}

# =============================================================================
# CONFIGURAÇÕES DE THREAD POOL
# =============================================================================
# Configurações de thread pool para produção
spring.task.execution.pool.core-size=${THREAD_POOL_CORE:10}
spring.task.execution.pool.max-size=${THREAD_POOL_MAX:50}
spring.task.execution.pool.queue-capacity=${THREAD_POOL_QUEUE:200}
spring.task.execution.pool.keep-alive=${THREAD_POOL_KEEP_ALIVE:60s}

# =============================================================================
# CONFIGURAÇÕES DE VALIDAÇÃO
# =============================================================================
# Configurações de validação para produção
spring.validation.enabled=${VALIDATION_ENABLED:true}
validation.fail-fast=${VALIDATION_FAIL_FAST:false}

# =============================================================================
# CONFIGURAÇÕES DE MAPSTRUCT
# =============================================================================
# Configurações de MapStruct para produção
mapstruct.default-component-model=${MAPSTRUCT_COMPONENT_MODEL:spring}
mapstruct.unmapped-target-policy=${MAPSTRUCT_UNMAPPED_POLICY:IGNORE}

# =============================================================================
# CONFIGURAÇÕES DE FLYWAY
# =============================================================================
# Configurações de Flyway para produção
spring.flyway.enabled=${FLYWAY_ENABLED:true}
spring.flyway.baseline-on-migrate=${FLYWAY_BASELINE:true}
spring.flyway.validate-on-migrate=${FLYWAY_VALIDATE:false}

# =============================================================================
# CONFIGURAÇÕES DE OPENAPI
# =============================================================================
# Configurações de OpenAPI para produção
springdoc.api-docs.path=${OPENAPI_DOCS_PATH:/api-docs}
springdoc.swagger-ui.path=${OPENAPI_SWAGGER_PATH:/swagger-ui.html}
springdoc.swagger-ui.operations-sorter=${OPENAPI_OPERATIONS_SORTER:method}
springdoc.swagger-ui.tags-sorter=${OPENAPI_TAGS_SORTER:alpha}

# =============================================================================
# CONFIGURAÇÕES DE RESILIENCE4J
# =============================================================================
# Configurações de Resilience4j para produção
resilience4j.circuitbreaker.instances.default.sliding-window-size=${CIRCUIT_BREAKER_WINDOW:10}
resilience4j.retry.instances.default.wait-duration=${RETRY_WAIT_DURATION:100ms}
resilience4j.timelimiter.instances.default.timeout-duration=${TIMEOUT_DURATION:10s}

# =============================================================================
# CONFIGURAÇÕES DE OPENFEIGN
# =============================================================================
# Configurações de OpenFeign para produção
feign.client.config.default.connect-timeout=${FEIGN_CONNECT_TIMEOUT:5000}
feign.client.config.default.read-timeout=${FEIGN_READ_TIMEOUT:10000}
feign.client.config.default.logger-level=${FEIGN_LOG_LEVEL:basic}

# =============================================================================
# CONFIGURAÇÕES DE HIKARICP
# =============================================================================
# Configurações de HikariCP para produção
spring.datasource.hikari.connection-timeout=${HIKARI_CONNECTION_TIMEOUT:30000}
spring.datasource.hikari.idle-timeout=${HIKARI_IDLE_TIMEOUT:600000}
spring.datasource.hikari.max-lifetime=${HIKARI_MAX_LIFETIME:1800000}
spring.datasource.hikari.validation-timeout=${HIKARI_VALIDATION_TIMEOUT:5000}
spring.datasource.hikari.leak-detection-threshold=${HIKARI_LEAK_DETECTION:60000}

# =============================================================================
# CONFIGURAÇÕES DE HIBERNATE
# =============================================================================
# Configurações de Hibernate para produção
spring.jpa.properties.hibernate.jdbc.batch_size=${HIBERNATE_BATCH_SIZE:25}
spring.jpa.properties.hibernate.order_inserts=${HIBERNATE_ORDER_INSERTS:true}
spring.jpa.properties.hibernate.order_updates=${HIBERNATE_ORDER_UPDATES:true}
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=${HIBERNATE_BATCH_VERSIONED:true}
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=${HIBERNATE_DISABLE_AUTOCOMMIT:true}

# =============================================================================
# CONFIGURAÇÕES DE TOMCAT
# =============================================================================
# Configurações de Tomcat para produção
server.tomcat.threads.max=${TOMCAT_MAX_THREADS:200}
server.tomcat.threads.min-spare=${TOMCAT_MIN_THREADS:10}
server.tomcat.max-connections=${TOMCAT_MAX_CONNECTIONS:8192}
server.tomcat.accept-count=${TOMCAT_ACCEPT_COUNT:100}

# =============================================================================
# CONFIGURAÇÕES DE CACHE
# =============================================================================
# Configurações de cache para produção
spring.cache.type=${CACHE_TYPE:caffeine}
spring.cache.cache-names=${CACHE_NAMES:usuarios,cartoes,faturas,transacoes,relatorios,simulacoes}

# =============================================================================
# CONFIGURAÇÕES DE MONITORAMENTO DE SAÚDE
# =============================================================================
# Configurações de monitoramento de saúde para produção
management.health.db.enabled=${HEALTH_DB_ENABLED:true}
management.health.diskspace.enabled=${HEALTH_DISKSPACE_ENABLED:true}
management.health.defaults.enabled=${HEALTH_DEFAULTS_ENABLED:true}
management.health.circuitbreakers.enabled=${HEALTH_CIRCUITBREAKERS_ENABLED:true}

# =============================================================================
# CONFIGURAÇÕES DE MÉTRICAS PERSONALIZADAS
# =============================================================================
# Configurações de métricas personalizadas para produção
management.metrics.enable.jvm=${METRICS_JVM_ENABLED:true}
management.metrics.enable.process=${METRICS_PROCESS_ENABLED:true}
management.metrics.enable.system=${METRICS_SYSTEM_ENABLED:true}
management.metrics.enable.logback=${METRICS_LOGBACK_ENABLED:true}
management.metrics.enable.web=${METRICS_WEB_ENABLED:true}
management.metrics.enable.cache=${METRICS_CACHE_ENABLED:true}

# =============================================================================
# CONFIGURAÇÕES DE LOGGING ESTRUTURADO
# =============================================================================
# Configurações de logging estruturado para produção
logging.pattern.console=${LOG_PATTERN_CONSOLE:%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n}
logging.pattern.file=${LOG_PATTERN_FILE:%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n}

# =============================================================================
# CONFIGURAÇÕES DE ARQUIVO DE LOG
# =============================================================================
# Configurações de arquivo de log para produção
logging.file.name=${LOG_FILE_NAME:logs/consumo-esperto.log}
logging.logback.rollingpolicy.max-file-size=${LOG_MAX_FILE_SIZE:100MB}
logging.logback.rollingpolicy.max-history=${LOG_MAX_HISTORY:30}
logging.logback.rollingpolicy.total-size-cap=${LOG_TOTAL_SIZE_CAP:1GB}

# =============================================================================
# CONFIGURAÇÕES DE LOGS ESPECÍFICOS
# =============================================================================
# Configurações de logs específicos para produção
logging.level.com.consumoesperto.service.BankApiService=${BANK_API_LOG_LEVEL:INFO}
logging.level.com.consumoesperto.service.MercadoPagoService=${MERCADOPAGO_LOG_LEVEL:INFO}
logging.level.com.consumoesperto.service.ItauBankService=${ITAU_LOG_LEVEL:INFO}
logging.level.com.consumoesperto.security=${AUDIT_LOG_LEVEL:INFO}
logging.level.com.consumoesperto.controller=${CONTROLLER_LOG_LEVEL:INFO}

# =============================================================================
# CONFIGURAÇÕES DE SEGURANÇA DE CONTA
# =============================================================================
# Configurações de segurança de conta para produção
security.account.lockout.max-attempts=${ACCOUNT_LOCKOUT_MAX_ATTEMPTS:5}
security.account.lockout.duration=${ACCOUNT_LOCKOUT_DURATION:15m}
security.account.lockout.threshold=${ACCOUNT_LOCKOUT_THRESHOLD:3}

# =============================================================================
# CONFIGURAÇÕES DE SESSÃO DE SEGURANÇA
# =============================================================================
# Configurações de sessão de segurança para produção
security.session.max-sessions=${SESSION_MAX_SESSIONS:1}
security.session.invalidate-on-expiration=${SESSION_INVALIDATE_ON_EXPIRATION:true}
security.session.fixation-protection=${SESSION_FIXATION_PROTECTION:changeSessionId}

# =============================================================================
# CONFIGURAÇÕES DE SENHA
# =============================================================================
# Configurações de senha para produção
security.password.min-length=${PASSWORD_MIN_LENGTH:8}
security.password.require-uppercase=${PASSWORD_REQUIRE_UPPERCASE:true}
security.password.require-lowercase=${PASSWORD_REQUIRE_LOWERCASE:true}
security.password.require-numbers=${PASSWORD_REQUIRE_NUMBERS:true}
security.password.require-special-chars=${PASSWORD_REQUIRE_SPECIAL_CHARS:true}
security.password.history-size=${PASSWORD_HISTORY_SIZE:5}

# =============================================================================
# CONFIGURAÇÕES DE TOKEN
# =============================================================================
# Configurações de token para produção
security.token.refresh-token.enabled=${REFRESH_TOKEN_ENABLED:true}
security.token.refresh-token.expiration=${REFRESH_TOKEN_EXPIRATION:604800000}
security.token.blacklist.enabled=${TOKEN_BLACKLIST_ENABLED:true}

# =============================================================================
# CONFIGURAÇÕES DE AUDITORIA
# =============================================================================
# Configurações de auditoria para produção
spring.data.jpa.repositories.enabled=${JPA_REPOSITORIES_ENABLED:true}
spring.jpa.properties.hibernate.jdbc.time_zone=${HIBERNATE_TIMEZONE:UTC}

# =============================================================================
# CONFIGURAÇÕES DE VALIDAÇÃO DE BEANS
# =============================================================================
# Configurações de validação de beans para produção
spring.validation.enabled=${VALIDATION_ENABLED:true}

# =============================================================================
# CONFIGURAÇÕES DE COMPRESSÃO DE RESPOSTA HTTP
# =============================================================================
# Configurações de compressão de resposta HTTP para produção
server.compression.enabled=${COMPRESSION_ENABLED:true}
server.compression.mime-types=${COMPRESSION_MIME_TYPES:text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json}
server.compression.min-response-size=${COMPRESSION_MIN_SIZE:1024}

# =============================================================================
# CONFIGURAÇÕES DE SESSION
# =============================================================================
# Configurações de sessão para produção
server.servlet.session.timeout=${SESSION_TIMEOUT:30m}
server.servlet.session.cookie.http-only=${SESSION_HTTP_ONLY:true}
server.servlet.session.cookie.secure=${SESSION_SECURE:false}

# =============================================================================
# CONFIGURAÇÕES DE HEADERS DE SEGURANÇA
# =============================================================================
# Configurações de headers de segurança para produção
server.servlet.session.cookie.same-site=${SESSION_SAME_SITE:strict}

# =============================================================================
# CONFIGURAÇÕES DE TIMEOUT
# =============================================================================
# Configurações de timeout para produção
server.connection-timeout=${CONNECTION_TIMEOUT:20000}
server.tomcat.connection-timeout=${TOMCAT_CONNECTION_TIMEOUT:20000}
server.tomcat.keep-alive-timeout=${TOMCAT_KEEP_ALIVE_TIMEOUT:20000}

# =============================================================================
# CONFIGURAÇÕES DE COMPRESSÃO DE LOGS
# =============================================================================
# Configurações de compressão de logs para produção
logging.file.name=${LOG_FILE_NAME:logs/consumo-esperto.log}
logging.logback.rollingpolicy.max-file-size=${LOG_MAX_FILE_SIZE:100MB}
logging.logback.rollingpolicy.max-history=${LOG_MAX_HISTORY:30}

# =============================================================================
# CONFIGURAÇÕES DE MONITORAMENTO DE SAÚDE
# =============================================================================
# Configurações de monitoramento de saúde para produção
management.health.db.enabled=${HEALTH_DB_ENABLED:true}
management.health.diskspace.enabled=${HEALTH_DISKSPACE_ENABLED:true}
management.health.defaults.enabled=${HEALTH_DEFAULTS_ENABLED:true}

# =============================================================================
# CONFIGURAÇÕES DE MÉTRICAS PERSONALIZADAS
# =============================================================================
# Configurações de métricas personalizadas para produção
management.metrics.enable.jvm=${METRICS_JVM_ENABLED:true}
management.metrics.enable.process=${METRICS_PROCESS_ENABLED:true}
management.metrics.enable.system=${METRICS_SYSTEM_ENABLED:true}
management.metrics.enable.logback=${METRICS_LOGBACK_ENABLED:true}

# =============================================================================
# CONFIGURAÇÕES FINAIS
# =============================================================================
# Este arquivo contém todas as configurações necessárias para produção
# Para usar, configure as variáveis de ambiente acima
# Para desenvolvimento local, use application-dev.properties
