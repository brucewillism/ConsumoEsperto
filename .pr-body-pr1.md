## Summary
- Transcrição de áudio WhatsApp via `gpt-4o-transcribe` com `language: pt` (modelo configurável)
- `WhisperTranscriptionService` e `WhatsAppMediaService` centralizam STT e download de mídia
- Confirmação eco + *sim* antes de mutações vindas de voz (consignado reutiliza fluxo existente)
- Fallback amigável quando áudio falha; webhook Evolution mantém ACK + dedup + async

## Test plan
- [ ] Áudio de despesa com eco e confirmação antes de gravar
- [ ] Webhook reenviado processado uma vez (dedup)
- [ ] Áudio inaudível retorna fallback sem exceção
- [ ] mvn compile BUILD SUCCESS
